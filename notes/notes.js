/* DATA TYPES */

let object = {
    firstName: 'Chami',
    lastName: 'Ordonez',
    age: '25'
};

let everything_else = primitive;

let primitive = {
    1: 'number',
    2: 'string',
    3: 'boolean',
    4: 'undefined',
    5: 'null',
    6: 'symbol',
    7: 'BigInt'
};

let number = 23;

let string = 'String';

let boolean = true;

let undefined = 'Value taken by a variable that is not yet defined ("empty value") example: "let varibale;"';

let nullity = 'Also means "empty value"'

let symbol = 'Value that is unique and cannot be changed'

let bigint = 'Larger integers than the Number type can hold'